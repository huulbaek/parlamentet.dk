generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model afstemning {
  id              Int      @id(map: "idx_26310_pk_afstemning")
  nummer          Int
  konklusion      String?
  vedtaget        Boolean
  kommentar       String?
  moedeid         Int
  typeid          Int
  sagstrinid      Int?
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Aktoer {
  id              Int       @id(map: "idx_26320_PK_Aktør")
  typeid          Int
  gruppenavnkort  String?
  navn            String?
  fornavn         String?
  efternavn       String?
  biografi        String?
  periodeid       Int?
  opdateringsdato DateTime  @db.Timestamptz(6)
  startdato       DateTime? @db.Timestamptz(6)
  slutdato        DateTime? @db.Timestamptz(6)
}

model AktoerAktoer {
  id              BigInt    @id(map: "idx_26326_PK_AktørAktør") @default(autoincrement())
  fraaktoerid     Int
  tilaktoerid     Int
  startdato       DateTime? @db.Timestamptz(6)
  slutdato        DateTime? @db.Timestamptz(6)
  opdateringsdato DateTime  @db.Timestamptz(6)
  rolleid         Int
}

model AktoerAktoerRolle {
  id              Int      @id(map: "idx_26330_PK_AktørAktørRolle")
  rolle           String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Aktoertype {
  id              Int      @id(map: "idx_26335_PK_Aktørtype")
  type            String?
  opdateringsdato DateTime @db.Timestamptz(6)
}

model DokumentAktoer {
  id              BigInt   @id(map: "idx_26363_PK_DokumentAktør") @default(autoincrement())
  dokumentid      Int
  aktoerid        Int
  opdateringsdato DateTime @db.Timestamptz(6)
  rolleid         Int
}

model DokumentAktoerRolle {
  id              Int      @id(map: "idx_26367_PK_DokumentAktørRolle")
  rolle           String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Moede {
  id                   Int       @id(map: "idx_26431_PK_Møde")
  titel                String
  lokale               String?
  nummer               String?
  dagsordenurl         String?
  starttidsbemaerkning String?
  offentlighedskode    String
  dato                 DateTime? @db.Timestamptz(6)
  statusid             Int?
  typeid               Int?
  periodeid            Int
  opdateringsdato      DateTime  @db.Timestamptz(6)
}

model MoedeAktoer {
  id              BigInt   @id(map: "idx_26437_PK_MødeAktør") @default(autoincrement())
  moedeid         Int
  aktoerid        Int
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Moedestatus {
  id              Int      @id(map: "idx_26441_PK_Mødestatus")
  status          String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Moedetype {
  id              Int      @id(map: "idx_26446_PK_Mødetype")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model SagAktoer {
  id              BigInt   @id(map: "idx_26467_PK_Sagaktør") @default(autoincrement())
  aktoerid        Int
  sagid           Int
  opdateringsdato DateTime @db.Timestamptz(6)
  rolleid         Int
}

model SagAktoerRolle {
  id              Int      @id(map: "idx_26471_PK_SagAktørRolle")
  rolle           String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model SagstrinAktoer {
  id              BigInt   @id(map: "idx_26504_PK_Sagstrinaktør") @default(autoincrement())
  sagstrinid      Int
  aktoerid        Int
  opdateringsdato DateTime @db.Timestamptz(6)
  rolleid         Int
}

model SagstrinAktoerRolle {
  id              Int      @id(map: "idx_26508_PK_SagstrinAktørRolle")
  rolle           String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model refactorlog {
  operationkey String @id(map: "idx_26307_pk____refact__d3aeffdbad8cba18") @db.Uuid

  @@map("__refactorlog")
}

model afstemningstype {
  id              Int      @id(map: "idx_26315_pk_afstemningstype")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model dagsordenspunkt {
  id                Int       @id(map: "idx_26340_pk_dagsordenspunkt")
  koerebemaerkning  String?
  titel             String?
  kommentar         String?
  nummer            String?
  forhandlingskode  String?
  forhandling       String?
  superid           Int?
  sagstrinid        Int?
  moedeid           Int
  offentlighedskode String
  opdateringsdato   DateTime? @db.Timestamptz(6)
}

model dagsordenspunktdokument {
  id                BigInt   @id(map: "idx_26346_pk_dagsordenspunktdokument") @default(autoincrement())
  dokumentid        Int
  dagsordenspunktid Int
  note              String?
  opdateringsdato   DateTime @db.Timestamptz(6)
}

model dagsordenspunktsag {
  id                BigInt   @id(map: "idx_26353_pk_dagsordenspunktsag") @default(autoincrement())
  dagsordenspunktid Int
  sagid             Int
  opdateringsdato   DateTime @db.Timestamptz(6)
}

model dokument {
  id                    Int       @id(map: "idx_26357_pk_dokument")
  typeid                Int
  kategoriid            Int
  statusid              Int
  offentlighedskode     String
  titel                 String
  dato                  DateTime  @db.Timestamptz(6)
  modtagelsesdato       DateTime? @db.Timestamptz(6)
  frigivelsesdato       DateTime? @db.Timestamptz(6)
  paragraf              String?
  paragrafnummer        String?
  spoergsmaalsordlyd    String?
  spoergsmaalstitel     String?
  spoergsmaalsid        Int?
  procedurenummer       String?
  grundnotatstatus      String?
  dagsordenudgavenummer Int?      @db.SmallInt
  opdateringsdato       DateTime  @db.Timestamptz(6)
}

model dokumentkategori {
  id              Int      @id(map: "idx_26372_pk_dokumentkategori")
  kategori        String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model dokumentstatus {
  id              Int      @id(map: "idx_26377_pk_dokumentstatus")
  status          String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model dokumenttype {
  id              Int      @id(map: "idx_26382_pk_dokumenttype")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model emneord {
  id              Int      @id(map: "idx_26387_pk_emneord")
  typeid          Int
  emneord         String
  opdateringsdato DateTime @db.Timestamptz(6)

  @@index([typeid], map: "idx_26387_ix_fk_emneord_emneordstype")
}

model emneorddokument {
  id              BigInt   @id(map: "idx_26393_pk_emneorddokument") @default(autoincrement())
  emneordid       Int
  dokumentid      Int
  opdateringsdato DateTime @db.Timestamptz(6)
}

model emneordsag {
  id              BigInt   @id(map: "idx_26398_pk_emneordsag") @default(autoincrement())
  emneordid       Int
  sagid           Int
  opdateringsdato DateTime @db.Timestamptz(6)
}

model emneordstype {
  id              Int      @id(map: "idx_26402_pk_emneordstype")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model entitetbeskrivelse {
  id              BigInt    @id(map: "idx_26408_pk_entitetbeskrivelse") @default(autoincrement())
  entitetnavn     String?
  beskrivelse     String?
  opdateringsdato DateTime? @db.Timestamptz(6)
}

model fil {
  id              Int      @id(map: "idx_26414_pk_fil")
  dokumentid      Int
  titel           String?
  versionsdato    DateTime @db.Timestamptz(6)
  filurl          String
  opdateringsdato DateTime @db.Timestamptz(6)
  variantkode     String
  format          String
}

model idmap {
  id         Int
  originalid String
  entity     String

  @@id([id, entity], map: "idx_26419_pk_idmap")
  @@unique([originalid, entity], map: "idx_26419_ix_idmap_originalid_entity")
}

model kollonebeskrivelse {
  id              BigInt    @id(map: "idx_26425_pk_kollonebeskrivelse") @default(autoincrement())
  entitetnavn     String?
  kollonenavn     String?
  beskrivelse     String?
  opdateringsdato DateTime? @db.Timestamptz(6)
}

model omtryk {
  id              Int       @id(map: "idx_26451_pk_omtryk")
  dokumentid      Int
  dato            DateTime? @db.Timestamptz(6)
  begrundelse     String
  opdateringsdato DateTime  @db.Timestamptz(6)
}

model periode {
  id              Int      @id(map: "idx_26456_pk_periode")
  startdato       DateTime @db.Timestamptz(6)
  slutdato        DateTime @db.Timestamptz(6)
  type            String
  kode            String
  titel           String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sag {
  id                      Int       @id(map: "idx_26461_pk_sag")
  typeid                  Int
  kategoriid              Int?
  statusid                Int
  titel                   String
  titelkort               String
  offentlighedskode       String
  nummer                  String?
  nummerprefix            String?
  nummernumerisk          String?
  nummerpostfix           String?
  resume                  String?
  afstemningskonklusion   String?
  periodeid               Int
  afgoerelsesresultatkode String?
  baggrundsmateriale      String?
  opdateringsdato         DateTime  @db.Timestamptz(6)
  statsbudgetsag          Boolean?
  begrundelse             String?
  paragrafnummer          Int?
  paragraf                String?
  afgoerelsesdato         DateTime? @db.Timestamptz(6)
  afgoerelse              String?
  raadsmoededato          DateTime? @db.Timestamptz(6)
  lovnummer               String?
  lovnummerdato           DateTime? @db.Timestamptz(6)
  retsinformationsurl     String?
  fremsatundersagid       Int?
  deltundersagid          Int?

  @@index([deltundersagid], map: "idx_26461_ix_fk_deltundersag_sag")
  @@index([fremsatundersagid], map: "idx_26461_ix_fk_fremsatundersag_sag")
  @@index([kategoriid], map: "idx_26461_ix_fk_sag_sagskategori")
  @@index([statusid], map: "idx_26461_ix_fk_sag_sagsstatus")
  @@index([typeid], map: "idx_26461_ix_fk_sag_sagstype")
}

model sagdokument {
  id              BigInt    @id(map: "idx_26477_pk_sagdokument") @default(autoincrement())
  sagid           Int
  dokumentid      Int
  bilagsnummer    String?
  frigivelsesdato DateTime? @db.Timestamptz(6)
  opdateringsdato DateTime  @db.Timestamptz(6)
  rolleid         Int
}

model sagdokumentrolle {
  id              Int      @id(map: "idx_26483_pk_sagdokumentrolle")
  rolle           String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sagskategori {
  id              Int      @id(map: "idx_26488_pk_sagskategoriset")
  kategori        String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sagsstatus {
  id              Int      @id(map: "idx_26493_pk_sagsstatus")
  status          String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sagstrin {
  id                          Int       @id(map: "idx_26498_pk_sagstrin")
  titel                       String
  dato                        DateTime? @db.Timestamptz(6)
  sagid                       Int
  typeid                      Int
  folketingstidendeurl        String?
  folketingstidende           String?
  folketingstidendesidenummer String?
  statusid                    Int
  opdateringsdato             DateTime  @db.Timestamptz(6)

  @@index([sagid], map: "idx_26498_ix_fk_sagstrin_sag")
  @@index([statusid], map: "idx_26498_ix_fk_sagstrin_sagstrinsstatus")
  @@index([typeid], map: "idx_26498_ix_fk_sagstrin_sagstrinstype")
}

model sagstrindokument {
  id              BigInt   @id(map: "idx_26514_pk_sagstrindokument") @default(autoincrement())
  sagstrinid      Int
  dokumentid      Int
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sagstrinsstatus {
  id              Int      @id(map: "idx_26518_pk_sagstrinsstatus")
  status          String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sagstrinstype {
  id              Int      @id(map: "idx_26523_pk_sagstrinstypeset")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sagstype {
  id              Int      @id(map: "idx_26528_pk_sagstypeset")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model sambehandlinger {
  id                BigInt   @id(map: "idx_26534_pk_sagstrinsagstrin") @default(autoincrement())
  foerstesagstrinid Int
  andetsagstrinid   Int
  opdateringsdato   DateTime @db.Timestamptz(6)
}

model slettet {
  id              BigInt   @id(map: "idx_26539_pk_slettet") @default(autoincrement())
  slettetmapid    Int
  opdateringsdato DateTime @db.Timestamptz(6)
  objektid        Int
}

model slettetmap {
  id              BigInt   @id(map: "idx_26544_pk_slettetmap") @default(autoincrement())
  datatype        String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model stemme {
  id              BigInt   @id(map: "idx_26551_pk_stemmer") @default(autoincrement())
  typeid          Int?
  afstemningid    Int
  aktoerid        Int
  opdateringsdato DateTime @db.Timestamptz(6)

  @@unique([afstemningid, aktoerid], map: "idx_26551_AfstemningAktør")
}

model stemmetype {
  id              BigInt   @id(map: "idx_26556_pk_stemmetype") @default(autoincrement())
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model synclogger {
  id              BigInt    @id(map: "idx_26563_pk_mqalog") @default(autoincrement())
  entitet         String
  internid        Int
  eksternid       String
  opdateringsdato DateTime  @db.Timestamptz(6)
  antal           Int
  godkendtdato    DateTime? @db.Timestamptz(6)
}
