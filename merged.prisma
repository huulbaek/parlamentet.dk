generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Aktoer {
  FraAktoerAktoer AktoerAktoer[]   @relation("FraAktoerToAktoerAktoer")
  TilAktoerAktoer AktoerAktoer[]   @relation("TilAktoerToAktoerAktoer")
  DokumentAktoer  DokumentAktoer[]
  MoedeAktoer     MoedeAktoer[]
  SagAktoer       SagAktoer[]
  SagstrinAktoer  SagstrinAktoer[]
  Stemme          Stemme[]
  Aktoertype      Aktoertype?      @relation(fields: [typeid], references: [id])
  Periode         Periode?         @relation(fields: [periodeid], references: [id])
  id              Int              @id(map: "idx_69257_PK_Aktør")
  typeid          Int
  gruppenavnkort  String?
  navn            String?
  fornavn         String?
  efternavn       String?
  biografi        String?
  periodeid       Int?
  opdateringsdato DateTime         @db.Timestamptz(6)
  startdato       DateTime?        @db.Timestamptz(6)
  slutdato        DateTime?        @db.Timestamptz(6)
}

model AktoerAktoer {
  FraAktoer         Aktoer?            @relation("FraAktoerToAktoerAktoer", fields: [fraaktoerid], references: [id])
  TilAktoer         Aktoer?            @relation("TilAktoerToAktoerAktoer", fields: [tilaktoerid], references: [id])
  AktoerAktoerRolle AktoerAktoerRolle? @relation(fields: [rolleid], references: [id])
  id                BigInt             @id(map: "idx_69263_PK_AktørAktør") @default(autoincrement())
  fraaktoerid       Int
  tilaktoerid       Int
  startdato         DateTime?          @db.Timestamptz(6)
  slutdato          DateTime?          @db.Timestamptz(6)
  opdateringsdato   DateTime           @db.Timestamptz(6)
  rolleid           Int
}

model AktoerAktoerRolle {
  AktoerAktoer    AktoerAktoer[]
  id              Int            @id(map: "idx_69267_PK_AktørAktørRolle")
  rolle           String
  opdateringsdato DateTime       @db.Timestamptz(6)
}

model Aktoertype {
  Aktoer          Aktoer[]
  id              Int      @id(map: "idx_69272_PK_Aktørtype")
  type            String?
  opdateringsdato DateTime @db.Timestamptz(6)
}

model DokumentAktoer {
  Aktoer              Aktoer?              @relation(fields: [aktoerid], references: [id])
  Dokument            Dokument?            @relation(fields: [dokumentid], references: [id])
  DokumentAktoerRolle DokumentAktoerRolle? @relation(fields: [rolleid], references: [id])
  id                  BigInt               @id(map: "idx_69300_PK_DokumentAktør") @default(autoincrement())
  dokumentid          Int
  aktoerid            Int
  opdateringsdato     DateTime             @db.Timestamptz(6)
  rolleid             Int
}

model DokumentAktoerRolle {
  DokumentAktoer  DokumentAktoer[]
  id              Int              @id(map: "idx_69304_PK_DokumentAktørRolle")
  rolle           String
  opdateringsdato DateTime         @db.Timestamptz(6)
}

model Moede {
  Afstemning           Afstemning[]
  Dagsordenspunkt      Dagsordenspunkt[]
  MoedeAktoer          MoedeAktoer[]
  Moedestatus          Moedestatus?      @relation(fields: [statusid], references: [id])
  Moedetype            Moedetype?        @relation(fields: [typeid], references: [id])
  Periode              Periode?          @relation(fields: [periodeid], references: [id])
  id                   Int               @id(map: "idx_69368_PK_Møde")
  titel                String
  lokale               String?
  nummer               String?
  dagsordenurl         String?
  starttidsbemaerkning String?
  offentlighedskode    String
  dato                 DateTime?         @db.Timestamptz(6)
  statusid             Int?
  typeid               Int?
  periodeid            Int
  opdateringsdato      DateTime          @db.Timestamptz(6)
}

model MoedeAktoer {
  Aktoer          Aktoer?  @relation(fields: [aktoerid], references: [id])
  Moede           Moede?   @relation(fields: [moedeid], references: [id])
  id              BigInt   @id(map: "idx_69374_PK_MødeAktør") @default(autoincrement())
  moedeid         Int
  aktoerid        Int
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Moedestatus {
  Moede           Moede[]
  id              Int      @id(map: "idx_69378_PK_Mødestatus")
  status          String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Moedetype {
  Moede           Moede[]
  id              Int      @id(map: "idx_69383_PK_Mødetype")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model SagAktoer {
  Aktoer          Aktoer?         @relation(fields: [aktoerid], references: [id])
  Sag             Sag?            @relation(fields: [sagid], references: [id])
  SagAktoerRolle  SagAktoerRolle? @relation(fields: [rolleid], references: [id])
  id              BigInt          @id(map: "idx_69404_PK_Sagaktør") @default(autoincrement())
  aktoerid        Int
  sagid           Int
  opdateringsdato DateTime        @db.Timestamptz(6)
  rolleid         Int
}

model SagAktoerRolle {
  SagAktoer       SagAktoer[]
  id              Int         @id(map: "idx_69408_PK_SagAktørRolle")
  rolle           String
  opdateringsdato DateTime    @db.Timestamptz(6)
}

model SagstrinAktoer {
  Aktoer              Aktoer?              @relation(fields: [aktoerid], references: [id])
  Sagstrin            Sagstrin?            @relation(fields: [sagstrinid], references: [id])
  SagstrinAktoerRolle SagstrinAktoerRolle? @relation(fields: [rolleid], references: [id])
  id                  BigInt               @id(map: "idx_69441_PK_Sagstrinaktør") @default(autoincrement())
  sagstrinid          Int
  aktoerid            Int
  opdateringsdato     DateTime             @db.Timestamptz(6)
  rolleid             Int
}

model SagstrinAktoerRolle {
  SagstrinAktoer  SagstrinAktoer[]
  id              Int              @id(map: "idx_69445_PK_SagstrinAktørRolle")
  rolle           String
  opdateringsdato DateTime         @db.Timestamptz(6)
}

model refactorlog {
  operationkey String @id(map: "idx_69244_pk____refact__d3aeffdbad8cba18") @db.Uuid

  @@map("__refactorlog")
}

model Afstemning {
  Stemme          Stemme[]
  Afstemningstype Afstemningstype? @relation(map: "Afstemning_Afstemningstype_fkey", fields: [typeid], references: [id])
  Moede           Moede?           @relation(map: "Afstemning_Moede_fkey", fields: [moedeid], references: [id])
  Sagstrin        Sagstrin?        @relation(map: "Afstemning_Sagstrin_fkey", fields: [sagstrinid], references: [id])
  id              Int              @id(map: "idx_69247_pk_afstemning")
  nummer          Int
  konklusion      String?
  vedtaget        Boolean
  kommentar       String?
  moedeid         Int
  typeid          Int
  sagstrinid      Int?
  opdateringsdato DateTime         @db.Timestamptz(6)

  @@map("afstemning")
}

model Afstemningstype {
  Afstemning      Afstemning[]
  id              Int          @id(map: "idx_69252_pk_afstemningstype")
  type            String
  opdateringsdato DateTime     @db.Timestamptz(6)

  @@map("afstemningstype")
}

model Dagsordenspunkt {
  Dagsordenspunktdelti    Dagsordenspunkt[]         @relation("DagsordenspunktHierarchy")
  DagsordenspunktDokument DagsordenspunktDokument[]
  DagsordenspunktSag      DagsordenspunktSag[]
  DeltfraDagsordenspunkt  Dagsordenspunkt?          @relation("DagsordenspunktHierarchy", fields: [superid], references: [id])
  Moede                   Moede?                    @relation(fields: [moedeid], references: [id])
  Sagstrin                Sagstrin?                 @relation(fields: [sagstrinid], references: [id])
  id                      Int                       @id(map: "idx_69277_pk_dagsordenspunkt")
  koerebemaerkning        String?
  titel                   String?
  kommentar               String?
  nummer                  String?
  forhandlingskode        String?
  forhandling             String?
  superid                 Int?
  sagstrinid              Int?
  moedeid                 Int
  offentlighedskode       String
  opdateringsdato         DateTime?                 @db.Timestamptz(6)

  @@map("dagsordenspunkt")
}

model DagsordenspunktDokument {
  Dagsordenspunkt   Dagsordenspunkt? @relation(fields: [dagsordenspunktid], references: [id])
  Dokument          Dokument?        @relation(fields: [dokumentid], references: [id])
  id                BigInt           @id(map: "idx_69283_pk_dagsordenspunktdokument") @default(autoincrement())
  dokumentid        Int
  dagsordenspunktid Int
  note              String?
  opdateringsdato   DateTime         @db.Timestamptz(6)

  @@map("dagsordenspunktdokument")
}

model DagsordenspunktSag {
  Dagsordenspunkt   Dagsordenspunkt? @relation(fields: [dagsordenspunktid], references: [id])
  Sag               Sag?             @relation(fields: [sagid], references: [id])
  id                BigInt           @id(map: "idx_69290_pk_dagsordenspunktsag") @default(autoincrement())
  dagsordenspunktid Int
  sagid             Int
  opdateringsdato   DateTime         @db.Timestamptz(6)

  @@map("dagsordenspunktsag")
}

model Dokument {
  DagsordenspunktDokument DagsordenspunktDokument[]
  SvarDokumenter          Dokument[]                @relation("SvarSpoergsmaalRelation")
  SpoergsmaalsDokument    Dokument?                 @relation("SvarSpoergsmaalRelation", fields: [spoergsmaalsid], references: [id])
  DokumentAktoer          DokumentAktoer[]
  EmneordDokument         EmneordDokument[]
  Fil                     Fil[]
  Omtryk                  Omtryk[]
  SagDokument             SagDokument[]
  SagstrinDokument        SagstrinDokument[]
  Dokumentkategori        Dokumentkategori?         @relation(fields: [kategoriid], references: [id])
  Dokumentstatus          Dokumentstatus?           @relation(fields: [statusid], references: [id])
  Dokumenttype            Dokumenttype?             @relation(fields: [typeid], references: [id])
  id                      Int                       @id(map: "idx_69294_pk_dokument")
  typeid                  Int
  kategoriid              Int
  statusid                Int
  offentlighedskode       String
  titel                   String
  dato                    DateTime                  @db.Timestamptz(6)
  modtagelsesdato         DateTime?                 @db.Timestamptz(6)
  frigivelsesdato         DateTime?                 @db.Timestamptz(6)
  paragraf                String?
  paragrafnummer          String?
  spoergsmaalsordlyd      String?
  spoergsmaalstitel       String?
  spoergsmaalsid          Int?
  procedurenummer         String?
  grundnotatstatus        String?
  dagsordenudgavenummer   Int?                      @db.SmallInt
  opdateringsdato         DateTime                  @db.Timestamptz(6)

  @@map("dokument")
}

model Dokumentkategori {
  Dokument        Dokument[]
  id              Int        @id(map: "idx_69309_pk_dokumentkategori")
  kategori        String
  opdateringsdato DateTime   @db.Timestamptz(6)

  @@map("dokumentkategori")
}

model Dokumentstatus {
  Dokument        Dokument[]
  id              Int        @id(map: "idx_69314_pk_dokumentstatus")
  status          String
  opdateringsdato DateTime   @db.Timestamptz(6)

  @@map("dokumentstatus")
}

model Dokumenttype {
  Dokument        Dokument[]
  id              Int        @id(map: "idx_69319_pk_dokumenttype")
  type            String
  opdateringsdato DateTime   @db.Timestamptz(6)

  @@map("dokumenttype")
}

model Emneord {
  EmneordDokument EmneordDokument[]
  EmneordSag      EmneordSag[]
  Emneordstype    Emneordstype?     @relation(fields: [typeid], references: [id])
  id              Int               @id(map: "idx_69324_pk_emneord")
  typeid          Int
  emneord         String
  opdateringsdato DateTime          @db.Timestamptz(6)

  @@index([typeid], map: "idx_69324_ix_fk_emneord_emneordstype")
  @@map("emneord")
}

model EmneordDokument {
  Dokument        Dokument? @relation(fields: [dokumentid], references: [id])
  Emneord         Emneord?  @relation(fields: [emneordid], references: [id])
  id              BigInt    @id(map: "idx_69330_pk_emneorddokument") @default(autoincrement())
  emneordid       Int
  dokumentid      Int
  opdateringsdato DateTime  @db.Timestamptz(6)

  @@map("emneorddokument")
}

model EmneordSag {
  Emneord         Emneord? @relation(fields: [emneordid], references: [id])
  Sag             Sag?     @relation(fields: [sagid], references: [id])
  id              BigInt   @id(map: "idx_69335_pk_emneordsag") @default(autoincrement())
  emneordid       Int
  sagid           Int
  opdateringsdato DateTime @db.Timestamptz(6)

  @@map("emneordsag")
}

model Emneordstype {
  Emneord         Emneord[]
  id              Int       @id(map: "idx_69339_pk_emneordstype")
  type            String
  opdateringsdato DateTime  @db.Timestamptz(6)

  @@map("emneordstype")
}

model entitetbeskrivelse {
  id              BigInt    @id(map: "idx_69345_pk_entitetbeskrivelse") @default(autoincrement())
  entitetnavn     String?
  beskrivelse     String?
  opdateringsdato DateTime? @db.Timestamptz(6)
}

model Fil {
  Dokument        Dokument? @relation(fields: [dokumentid], references: [id])
  id              Int       @id(map: "idx_69351_pk_fil")
  dokumentid      Int
  titel           String?
  versionsdato    DateTime  @db.Timestamptz(6)
  filurl          String
  opdateringsdato DateTime  @db.Timestamptz(6)
  variantkode     String
  format          String

  @@map("fil")
}

model idmap {
  id         Int
  originalid String
  entity     String

  @@id([id, entity], map: "idx_69356_pk_idmap")
  @@unique([originalid, entity], map: "idx_69356_ix_idmap_originalid_entity")
}

model kollonebeskrivelse {
  id              BigInt    @id(map: "idx_69362_pk_kollonebeskrivelse") @default(autoincrement())
  entitetnavn     String?
  kollonenavn     String?
  beskrivelse     String?
  opdateringsdato DateTime? @db.Timestamptz(6)
}

model Omtryk {
  Dokument        Dokument? @relation(fields: [dokumentid], references: [id])
  id              Int       @id(map: "idx_69388_pk_omtryk")
  dokumentid      Int
  dato            DateTime? @db.Timestamptz(6)
  begrundelse     String
  opdateringsdato DateTime  @db.Timestamptz(6)

  @@map("omtryk")
}

model Periode {
  Aktoer          Aktoer[]
  Moede           Moede[]
  Sag             Sag[]
  id              Int      @id(map: "idx_69393_pk_periode")
  startdato       DateTime @db.Timestamptz(6)
  slutdato        DateTime @db.Timestamptz(6)
  type            String
  kode            String
  titel           String
  opdateringsdato DateTime @db.Timestamptz(6)

  @@map("periode")
}

model Sag {
  DagsordenspunktSag      DagsordenspunktSag[]
  EmneordSag              EmneordSag[]
  Sagerdelti              Sag[]                @relation("SagerdeltiRelation")
  Sagerfremsatunder       Sag[]                @relation("SagerfremsatunderRelation")
  DeltfraSag              Sag?                 @relation("SagerdeltiRelation", fields: [deltundersagid], references: [id])
  FremsatunderSag         Sag?                 @relation("SagerfremsatunderRelation", fields: [fremsatundersagid], references: [id])
  SagAktoer               SagAktoer[]
  SagDokument             SagDokument[]
  Sagstrin                Sagstrin[]
  Periode                 Periode?             @relation(fields: [periodeid], references: [id])
  Sagskategori            Sagskategori?        @relation(fields: [kategoriid], references: [id])
  Sagsstatus              Sagsstatus?          @relation(fields: [statusid], references: [id])
  Sagstype                Sagstype?            @relation(fields: [typeid], references: [id])
  id                      Int                  @id(map: "idx_69398_pk_sag")
  typeid                  Int
  kategoriid              Int?
  statusid                Int
  titel                   String
  titelkort               String
  offentlighedskode       String
  nummer                  String?
  nummerprefix            String?
  nummernumerisk          String?
  nummerpostfix           String?
  resume                  String?
  afstemningskonklusion   String?
  periodeid               Int
  afgoerelsesresultatkode String?
  baggrundsmateriale      String?
  opdateringsdato         DateTime             @db.Timestamptz(6)
  statsbudgetsag          Boolean?
  begrundelse             String?
  paragrafnummer          Int?
  paragraf                String?
  afgoerelsesdato         DateTime?            @db.Timestamptz(6)
  afgoerelse              String?
  raadsmoededato          DateTime?            @db.Timestamptz(6)
  lovnummer               String?
  lovnummerdato           DateTime?            @db.Timestamptz(6)
  retsinformationsurl     String?
  fremsatundersagid       Int?
  deltundersagid          Int?

  @@index([deltundersagid], map: "idx_69398_ix_fk_deltundersag_sag")
  @@index([fremsatundersagid], map: "idx_69398_ix_fk_fremsatundersag_sag")
  @@index([kategoriid], map: "idx_69398_ix_fk_sag_sagskategori")
  @@index([statusid], map: "idx_69398_ix_fk_sag_sagsstatus")
  @@index([typeid], map: "idx_69398_ix_fk_sag_sagstype")
  @@map("sag")
}

model SagDokument {
  Dokument         Dokument?         @relation(fields: [dokumentid], references: [id])
  Sag              Sag?              @relation(fields: [sagid], references: [id])
  SagDokumentRolle SagDokumentRolle? @relation(fields: [rolleid], references: [id])
  id               BigInt            @id(map: "idx_69414_pk_sagdokument") @default(autoincrement())
  sagid            Int
  dokumentid       Int
  bilagsnummer     String?
  frigivelsesdato  DateTime?         @db.Timestamptz(6)
  opdateringsdato  DateTime          @db.Timestamptz(6)
  rolleid          Int

  @@map("sagdokument")
}

model SagDokumentRolle {
  SagDokument     SagDokument[]
  id              Int           @id(map: "idx_69420_pk_sagdokumentrolle")
  rolle           String
  opdateringsdato DateTime      @db.Timestamptz(6)

  @@map("sagdokumentrolle")
}

model Sagskategori {
  Sag             Sag[]
  id              Int      @id(map: "idx_69425_pk_sagskategoriset")
  kategori        String
  opdateringsdato DateTime @db.Timestamptz(6)

  @@map("sagskategori")
}

model Sagsstatus {
  Sag             Sag[]
  id              Int      @id(map: "idx_69430_pk_sagsstatus")
  status          String
  opdateringsdato DateTime @db.Timestamptz(6)

  @@map("sagsstatus")
}

model Sagstrin {
  Afstemning                        Afstemning[]
  Dagsordenspunkt                   Dagsordenspunkt[]
  SagstrinAktoer                    SagstrinAktoer[]
  SagstrinDokument                  SagstrinDokument[]
  Sambehandlinger_foerstesagstrinid Sambehandlinger[]  @relation("FoersteSagstrin", map: "FoersteSagstrin_fkey")
  Sambehandlinger_andetsagstrinid   Sambehandlinger[]  @relation("AndetSagstrin", map: "AndetSagstrin_fkey")
  Sag                               Sag?               @relation(fields: [sagid], references: [id])
  Sagstrinsstatus                   Sagstrinsstatus?   @relation(fields: [statusid], references: [id])
  Sagstrinstype                     Sagstrinstype?     @relation(fields: [typeid], references: [id])
  id                                Int                @id(map: "idx_69435_pk_sagstrin")
  titel                             String
  dato                              DateTime?          @db.Timestamptz(6)
  sagid                             Int
  typeid                            Int
  folketingstidendeurl              String?
  folketingstidende                 String?
  folketingstidendesidenummer       String?
  statusid                          Int
  opdateringsdato                   DateTime           @db.Timestamptz(6)

  @@index([sagid], map: "idx_69435_ix_fk_sagstrin_sag")
  @@index([statusid], map: "idx_69435_ix_fk_sagstrin_sagstrinsstatus")
  @@index([typeid], map: "idx_69435_ix_fk_sagstrin_sagstrinstype")
  @@map("sagstrin")
}

model SagstrinDokument {
  Dokument        Dokument? @relation(fields: [dokumentid], references: [id], map: "SagsstrinDokument_Dokument_fkey")
  Sagstrin        Sagstrin? @relation(fields: [sagstrinid], references: [id], map: "SagstrinDokument_Sagstrin_fkey")
  id              BigInt    @id(map: "idx_69451_pk_sagstrindokument") @default(autoincrement())
  sagstrinid      Int
  dokumentid      Int
  opdateringsdato DateTime  @db.Timestamptz(6)

  @@map("sagstrindokument")
}

model Sagstrinsstatus {
  Sagstrin        Sagstrin[]
  id              Int        @id(map: "idx_69455_pk_sagstrinsstatus")
  status          String
  opdateringsdato DateTime   @db.Timestamptz(6)

  @@map("sagstrinsstatus")
}

model Sagstrinstype {
  Sagstrin        Sagstrin[]
  id              Int        @id(map: "idx_69460_pk_sagstrinstypeset")
  type            String
  opdateringsdato DateTime   @db.Timestamptz(6)

  @@map("sagstrinstype")
}

model Sagstype {
  Sag             Sag[]
  id              Int      @id(map: "idx_69465_pk_sagstypeset")
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)

  @@map("sagstype")
}

model Sambehandlinger {
  FoersteSagstrin   Sagstrin? @relation(name: "FoersteSagstrin", fields: [foerstesagstrinid], references: [id])
  AndetSagstrin     Sagstrin? @relation(name: "AndetSagstrin", fields: [andetsagstrinid], references: [id])
  id                BigInt    @id(map: "idx_69471_pk_sagstrinsagstrin") @default(autoincrement())
  foerstesagstrinid Int
  andetsagstrinid   Int
  opdateringsdato   DateTime  @db.Timestamptz(6)

  @@map("sambehandlinger")
}

model slettet {
  id              BigInt   @id(map: "idx_69476_pk_slettet") @default(autoincrement())
  slettetmapid    Int
  opdateringsdato DateTime @db.Timestamptz(6)
  objektid        Int
}

model slettetmap {
  id              BigInt   @id(map: "idx_69481_pk_slettetmap") @default(autoincrement())
  datatype        String
  opdateringsdato DateTime @db.Timestamptz(6)
}

model Stemme {
  Afstemning      Afstemning? @relation(fields: [afstemningid], references: [id])
  Aktoer          Aktoer?     @relation(fields: [aktoerid], references: [id])
  // Stemmetype      Stemmetype? @relation(fields: [typeid], references: [id])
  id              BigInt      @id(map: "idx_69488_pk_stemmer") @default(autoincrement())
  typeid          Int?
  afstemningid    Int
  aktoerid        Int
  opdateringsdato DateTime    @db.Timestamptz(6)

  @@unique([afstemningid, aktoerid], map: "idx_69488_AfstemningAktør")
  @@map("stemme")
}

model Stemmetype {
  // Stemme          Stemme[]
  id              BigInt   @id(map: "idx_69493_pk_stemmetype") @default(autoincrement())
  type            String
  opdateringsdato DateTime @db.Timestamptz(6)

  @@map("stemmetype")
}

model synclogger {
  id              BigInt    @id(map: "idx_69500_pk_mqalog") @default(autoincrement())
  entitet         String
  internid        Int
  eksternid       String
  opdateringsdato DateTime  @db.Timestamptz(6)
  antal           Int
  godkendtdato    DateTime? @db.Timestamptz(6)
}
